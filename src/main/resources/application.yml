spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ah
    username: ah
    password: ah123!
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
